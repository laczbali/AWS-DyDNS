{
    "$schema": "http://json-schema.org/draft-07/schema",

    "properties": {

        "cli_profile": {
            "type": "string",
            "description": "Which AWSCLI profile should the script use"
        },

        "hosted_zone_id": {
            "type": "string",
            "description": "Which hosted zone does the record belong to"
        },

        "record_name": {
            "type": "string",
            "description": "The specfific record you wish to update, within the hosted zone"
        }

    },

    "required": ["hosted_zone_id", "cli_profile", "record_name"]
}